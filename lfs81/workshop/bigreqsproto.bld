############# Building package
cd $UNPACKDIR

packagedir=bigreqsproto-1.1.2
  ./configure $XORG_CONFIG
  
 make -j1 install
  
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit