############# Building package
cd $UNPACKDIR

sed 's@/usr/local@/usr@g' Makefile.unix > Makefile &&
make
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit