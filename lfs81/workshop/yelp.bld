############# Building package
cd $UNPACKDIR

./configure --prefix=/usr --disable-static &&
make
make -j1 install &&
update-desktop-database
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit