############# Building package
cd $UNPACKDIR

packagedir=font-util-1.3.1
    ./configure $XORG_CONFIG
    make
    
 make -j1 install
  
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit