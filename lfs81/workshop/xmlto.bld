############# Building package
cd $UNPACKDIR

LINKS="/usr/bin/links" \
./configure --prefix=/usr &&

make
#make -k check || true
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit