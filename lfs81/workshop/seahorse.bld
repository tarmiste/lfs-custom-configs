############# Building package
cd $UNPACKDIR

sed -i -r 's:"(/apps):"/org/gnome\1:' data/*.xml &&

./configure --prefix=/usr  &&
make
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit