############# Building package
cd $UNPACKDIR

./configure --prefix=/usr --with-gobject &&
make
#make -k check || true
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit