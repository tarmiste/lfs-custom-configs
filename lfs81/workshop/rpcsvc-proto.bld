############# Building package
cd $UNPACKDIR

autoreconf -fi                &&
./configure --sysconfdir=/etc &&
make
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit