############# Building package
cd $UNPACKDIR

./configure $XORG_CONFIG &&
make
#LD_LIBRARY_PATH=$XORG_PREFIX/lib make -k check || true
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit