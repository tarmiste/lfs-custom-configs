############# Building package
cd $UNPACKDIR

./configure $XORG_CONFIG --with-xkb-rules-symlink=xorg &&
make
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit