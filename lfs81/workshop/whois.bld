############# Building package
cd $UNPACKDIR

make
make -j1 prefix=/usr install-whois
make -j1 prefix=/usr install-mkpasswd
make -j1 prefix=/usr install-pos
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit