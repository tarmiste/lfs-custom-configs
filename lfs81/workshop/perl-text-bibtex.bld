############# Building package
cd $UNPACKDIR

patch -Np1 -i ../Data-Uniqid-0.12-disable_failing_test-1.patch
perl Makefile.PL &&
make &&
make test
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit