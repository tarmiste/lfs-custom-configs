############# Building package
cd $UNPACKDIR

sed -i '/config.h/d' lib/libvolume_key.h &&

autoreconf -fiv           &&
./configure --prefix=/usr &&
make
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit