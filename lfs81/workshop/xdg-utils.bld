############# Building package
cd $UNPACKDIR

./configure --prefix=/usr --mandir=/usr/share/man &&
make
#make -k test || true
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit