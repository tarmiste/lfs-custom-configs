############# Building package
cd $UNPACKDIR

packagedir=presentproto-1.1
  ./configure $XORG_CONFIG
  
 make -j1 install
  
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit