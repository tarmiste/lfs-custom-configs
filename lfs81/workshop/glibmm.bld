############# Building package
cd $UNPACKDIR

sed -e '/^libdocdir =/ s/$(book_name)/glibmm-2.52.0/' \
    -i docs/Makefile.in
./configure --prefix=/usr &&
make
#make -k check || true
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit