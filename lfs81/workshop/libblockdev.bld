############# Building package
cd $UNPACKDIR

sh autogen.sh &&

./configure --prefix=/usr \
            --without-dm  \
            --sysconfdir=/etc &&
make
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit