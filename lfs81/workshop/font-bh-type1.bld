############# Building package
cd $UNPACKDIR

packagedir=font-bh-type1-1.0.3
    ./configure $XORG_CONFIG
    make
    
 make -j1 install
  
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit