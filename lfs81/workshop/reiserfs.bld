############# Building package
cd $UNPACKDIR

autoreconf -fiv             &&
./configure --prefix=/usr   \
            --sbindir=/sbin &&

make
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit