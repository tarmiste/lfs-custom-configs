############# Building package
cd $UNPACKDIR

./configure $XORG_CONFIG     \
            --docdir=/usr/share/doc/libxkbcommon-0.7.2 &&
make
#make -k check || true
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit