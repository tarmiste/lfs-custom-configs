############# Building package
cd $UNPACKDIR

./configure --prefix=$XORG_PREFIX --disable-static &&
make
make -j1 install
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit