############# Building package
cd $UNPACKDIR

python setup.py build
#make -k  || true
#make -k  || true
python setup.py install --optimize=1
ldconfig

cd $BUILD_DIR
[[ -n "$KEEP_FILES" ]] || rm -rf $UNPACKDIR unpacked

exit